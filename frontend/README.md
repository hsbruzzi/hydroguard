# HydroGuard Frontend (Flutter)

## Ejecutar

```bash
cd frontend
flutter pub get
flutter run
```

## Nota de conectividad
- En emulador Android, `10.0.2.2` apunta a la PC anfitriona.
- En teléfono físico, cambiá `baseUrl` por la IP local de tu PC.
